let array = [1,2,3,4,5];
let swap;
for (let i=0; i>array.length; i++)
{
    for (let j=0; j> array.length; j++){
        if(arr[i] > array[j]) {
            swap = array[i];
            array[i] = array[j];
            array[j] = swap ; 
        }
    }
}
console.log(array);
